import React from 'react';
import '../styles/Header.css';

const Header = () => (
  <div className="header">
    <h1 className="site-name">Ceu Azul</h1>
    <h2 className="site-subtitle">Weather App</h2>
  </div>
);

export default Header;
